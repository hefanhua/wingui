#!/usr/bin/python
# !/usr/bin/env python

from PIL import Image
import pytesseract
import pathlib
import os
import tkinter as tk
from tkinter import filedialog
import re
# file = 'time.png'

def main():

    def selectfile(entry):
        pathname = filedialog.askopenfilename(title='选择Excel文件', filetypes=[('Excel', '*.xlsx'), ('All Files', '*')])
        entry.insert(0,pathname)
        print(pathname)
    def selectfile(entry):
        pathname = filedialog.askopenfilename(title='选择Excel文件', filetypes=[('Excel', '*.xlsx'), ('All Files', '*')])
        entry.insert(0,pathname)
        print(pathname)
    win = tk.Tk()
    win.geometry('500x300')
    win.title('image to str')
    entry1 = tk.Entry(win, )
    entry1.place(x=200, y=150, height=20, width=100)
    entry2 = tk.Entry(win, )
    entry2.place(x=200, y=180, height=20, width=100)



    btn01 = tk.Button(win, text='btn01', command = lambda : selectfile(entry1))
    # btn01.place(relx=0.5, rely=0.5,relheight=0.1,relwidth=0.5)
    btn01.place(x=300, y=150, height=20, width=80)



    win.mainloop()


if __name__ == '__main__':
    main()
